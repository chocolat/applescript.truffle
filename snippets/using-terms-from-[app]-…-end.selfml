(name [using terms from [app] … end])

(trigger
    (tab terms))

(only-in source.applescript)

(snippet [using terms from %{1="application "%{2="Finder"}""}
	%{0="-- statements"}
end using terms from])